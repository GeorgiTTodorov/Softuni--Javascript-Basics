function projectsCreation(input) {
    let architectName = input[0];
    let hoursTotal = input[1] * 3;
    let projectsNumber = input[1];
    console.log(`The architect ${architectName} will need ${hoursTotal} hours to complete ${projectsNumber} project/s.`);
    
}

projectsCreation(["George ","4 "]);
projectsCreation(["Sanya ","9 "]);